name: HAndle Issues
on:
  issues:
    types: [opened, edited, closed]   
jobs:
  output-info:
    runs-on: ubuntu-latest
  
    steps:
      - name: Output events
        run: echo "${{ toJSON(github.event) }}"

